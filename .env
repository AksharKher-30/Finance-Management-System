# Environment configuration for Finance Management System
# Copy this file to .env and adjust values as needed.

# Mongo connection string (Atlas)
# If your password contains special characters, ensure it's URL-encoded and keep the quotes.
MONGO_URI="mongodb+srv://kherakshar42_db_user:oZ7JmnMXgxyN8n1L@cluster0.yjfdqbi.mongodb.net/finance_management?retryWrites=true&w=majority&appName=Cluster0"

# Explicit database name (used even if not present in URI)
DB_NAME=finance_management

# Server port override (optional)
PORT=5001

# Add future secrets/keys below as needed
